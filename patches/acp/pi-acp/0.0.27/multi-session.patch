diff -ruN original/dist/index.js patched/dist/index.js
--- original/dist/index.js
+++ patched/dist/index.js
@@ -1515,6 +1515,9 @@
   }
 }
 var pkg = readNearestPackageJson(import.meta.url);
+function shouldUseSingleLiveSession() {
+  return process.env.PI_ACP_SINGLE_LIVE_SESSION === "true";
+}
 var PiAcpAgent = class {
   conn;
   sessions = new SessionManager();
@@ -1637,7 +1640,9 @@
     });
     if (preludeText)
       session.setStartupInfo(preludeText);
-    this.sessions.closeAllExcept?.(session.sessionId);
+    if (shouldUseSingleLiveSession()) {
+      this.sessions.closeAllExcept?.(session.sessionId);
+    }
     const response = {
       sessionId: session.sessionId,
       models,
@@ -2108,7 +2113,9 @@
       proc,
       fileCommands
     });
-    this.sessions.closeAllExcept?.(session.sessionId);
+    if (shouldUseSingleLiveSession()) {
+      this.sessions.closeAllExcept?.(session.sessionId);
+    }
     this.store.upsert({
       sessionId: params.sessionId,
       cwd: params.cwd,
